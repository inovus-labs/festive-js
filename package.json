{
  "name": "festive-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm -w package run build",
    "preview": "npm -w package run preview",
    "dev": "npm -w package run dev"
  }
}